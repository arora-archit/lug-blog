<section class="comments-section">
	<div class="header">
		<span class="header-title">
			<img src="/comment.svg" width="24" />
			Add A Comment
		</span>

		<a href="#" class="signin">
			<img src="/signin.svg" width="20" />
			Sign in with google
		</a>
	</div>

	<textarea class="comment-box"></textarea>

	<div class="post-row">
		<button class="post-btn">Post <img src="/post.svg" width="20" /></button
		>
	</div>
</section>

<section class="comments-list">
	<div class="comment">
		<div class="avatar"></div>
		<div class="comment-content">
			<div class="comment-author">Penguin Syndicate Member</div>
			<div class="comment-text">This is a comment.</div>
		</div>
	</div>
</section>

<style>
	.comments-section {
		margin: 20px;
		background: #3b4252;
		border: 3px solid #1f2430;
		box-shadow: 6px 6px black;
		font-family: "Space Mono", monospace;
	}

	.header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		background: #3b4252;
		padding: 10px 14px;
		border-bottom: 3px solid #2e3440;
	}

	.header-title {
		display: flex;
		align-items: center;
		gap: 10px;
		color: white;
		font-size: 22px;
	}

	.signin {
		display: flex;
		align-items: center;
		gap: 8px;
		background: #00c764;
		color: black;
		text-decoration: none;
		padding: 4px 10px;
		box-shadow: 6px 6px black;
	}

	.signin:hover {
		transform: translate(1px, 1px);
		box-shadow: 4px 4px black;
	}

	.comment-box {
		display: block;
		width: calc(100% - 50px);
		height: 110px;
		margin: 14px;
		background: #6b7280;
		box-shadow: 6px 6px black;
		padding: 10px;
		color: white;
		font-size: 16px;
		outline: none;
		resize: none;
	}

	.post-row {
		display: flex;
		justify-content: flex-end;
		padding: 0 14px 14px;
	}

	.post-btn {
		display: flex;
		align-items: center;
		gap: 8px;
		background: #00a9f3;
		border: none;
		padding: 8px 20px;
		font-size: 18px;
		box-shadow: 6px 6px black;
	}

	.post-btn:hover {
		transform: translate(1px, 1px);
		box-shadow: 4px 4px black;
	}

	.comments-list {
		margin: 20px;
		margin-left: 100px;
		display: flex;
		flex-direction: column;
		gap: 12px;
	}

	.comment {
		display: flex;
		gap: 12px;
		background: #636b7b;
		border: 3px solid #1f2430;
		box-shadow: 6px 6px black;
		padding: 12px;
		font-family: "Space Mono", monospace;
		color: white;
	}

	.avatar {
		width: 48px;
		height: 48px;
		background: #d1d5db;
		border: 3px solid #1f2430;
		box-shadow: 4px 4px black;
		flex-shrink: 0;
	}

	.comment-content {
		display: flex;
		flex-direction: column;
		gap: 6px;
	}

	.comment-author {
		font-size: 14px;
		color: #d1d5db;
	}

	.comment-text {
		font-size: 16px;
	}

	@media (max-width: 820px) {
		.comments-section {
			margin: 20px;
		}

		.comments-list {
			margin-left: 20px;
		}

		.header {
			flex-direction: column;
			gap: 15px;
			align-items: stretch;
		}

		.header-title {
			justify-content: center;
		}

		.signin {
			justify-content: center;
		}

		.comment-box {
			width: calc(100% - 50px);
		}
	}
	</style>